 version: '2'
 services:
   web:
     build: .
     command: bash -c "source ~/venv/bin/activate && python manage.py runserver 0.0.0.0:8000"
     volumes:
       - .:/code
     ports:
       - "8000:8000"
